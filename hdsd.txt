Hướng dẫn sử dụng tool spam comment vip đa luồng mobile.

Hướng dẫn setup chạy tool trên termux:
	- tải và cài đặt ứng dụng termux trên chplay về
	- cấp quền bộ nhớ cho termux:
		+ cách 1: vào cài đặt -> tìm đến ứng dụng -> tìm termux và nhấn vào -> chọn vào quyền -> sau đó cấp quền bộ nhớ cho nó là xong
		+ cách 2: vào termux chạy lệnh này: termux-setup-storage
	- cài đặt các thư viện cần thiết:
		+ bạn copy chuỗi lệnh này rồi vào termux nhấn dữ màn hình 3s sau đó chọn path. lưu ý: trong quá trình chạy lệnh dưới mà termux có hỏi (y/n) thì chọn y rồi enter nhé
		lệnh: termux-setup-storage && apt update && apt upgrade && pkg install python && pkg install nano && pkg install git && pip install telethon && pip install requests && pip install bs4 && pip install rsa && pip install pyaes && pip install async_generator && pip install colorama && pip install --upgrade pip
	- cách chạy tool:
		+ sử dụng lệnh cd để di chuyển đến thư mục chứa tool.
		vd: cd /sdcard/download/tool_spam_comment_mb
		ở ví dụ trên mình giải nén tool và để file tool tại thư mục download và mình truy cập vào thư mục tool có tên tool_spam_comment_mb, khi bạn đã truy cập vào thư mục chứa tool bạn chạy lệnh ls dể hiện ra các file có trong tư mục đó và trong thư mục đó có file tool_spam_vip.py là đúng nha
		+ khi đã di chuyển vào thư mục tool, bạn chạy lệnh pyhon tool_spam_vip.py là xong.
	
	- cách thêm token:
		+ bạn thêm token tại file token (mỗi token của một nick facebook một dòng nhé), thêm xong nhớ nhấn save nha =)

